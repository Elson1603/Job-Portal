Django==5.1.5 # Based on "Generated by 'django-admin startproject' using Django 5.1.5."
social-auth-app-django # Required because 'social_django' is in INSTALLED_APPS
social-auth-core # social-auth-app-django depends on this, so it's good to include explicitly or let pip resolve
# Add Gunicorn for production deployment (recommended for Render)
gunicorn
WeasyPrint
whitenoise
# If you plan to use a PostgreSQL database on Render (highly recommended over SQLite for production):
# dj-database-url
# psycopg2-binary
# For serving static files in production (highly recommended for Render):
# whitenoise
